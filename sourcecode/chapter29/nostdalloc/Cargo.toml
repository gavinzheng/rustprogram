[package]
name = "nostd"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.86"
libc_alloc = "*"
libc-print="*"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"